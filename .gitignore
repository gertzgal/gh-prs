# Built binary at repo root (rebuilt locally; committed only at cutover)
/gh-prs

# Worker worktrees (created inside project for sandbox access)
/.worktrees/

# Cross-compile output
/dist/

# Internal planning/design docs — not part of the published repo
/planning/
/plans/
/docs/superpowers/

# OS cruft
.DS_Store

.env
.envrc

# Codex local workspace state
.codex/
